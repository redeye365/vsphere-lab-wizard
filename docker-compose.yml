services:
  zero-to-hero:
    build: .
    image: zero-to-hero:latest
    container_name: zero-to-hero
    ports:
      - "3000:3000"
    volumes:
      - ./scenarios:/app/scenarios
    environment:
      - PORT=3000
      - HOST=0.0.0.0
      - ADMIN_ENABLED=false
    restart: unless-stopped
